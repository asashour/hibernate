# Introduction

This repository shows a case where joining `root` of a subclass property is working with Hibernate ORM 6, but not 7.

## Branches
1. `main` branch hosts Hibernate ORM 7
2. `hibernate-orm-6` branch hosts Hibernate ORM 6